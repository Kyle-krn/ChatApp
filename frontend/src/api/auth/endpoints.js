const Endpoints = {
    AUTH: {
        LOGIN: '/auth/login',
        LOGOUT: '/auth/logout',
        CSRF_TOKEN: '/auth/csrf_cookie',
        CHECK_AUTH: '/auth/check_authenticated'
    }
};

export default Endpoints;